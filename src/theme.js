import { createMuiTheme } from '@material-ui/core/styles';

const palette = {
  primary: { main: '#F57C00', contrastText: '#FAFAFA' },
  secondary: { main: '#FF9800', contrastText: '#FAFAFA' }
};
const themeName = 'Gold Drop Pizazz Cow';

export default createMuiTheme({ palette, themeName });
